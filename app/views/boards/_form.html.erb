<%= form_with(model: @board, local: true, class: "ui form) do |f|"%>
<div class="field>
<%= f.label :title%>
<%= f.text_field :title, favorite, require:true, placeholder: "Title" %>
</div>
<%= f.submit class: "ui green button" %>
<% end %>
</div>